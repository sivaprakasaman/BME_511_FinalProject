function [env_cohere, tfs_cohere, s, phi, input_env, input_tfs] = getCoherence(bankedSig, input_fs, psth_pos, psth_neg, NFFT, CF);
%UNTITLED Summary of this function goes here
%   Detailed explanation goes here
    
    env_cohere = zeros(length(CF), 
    
    for i = 1:length(CF)
        
        
        
    end

end

